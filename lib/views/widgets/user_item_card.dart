import 'package:flutter/material.dart';

class UserItemCard extends StatelessWidget {
  const UserItemCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
